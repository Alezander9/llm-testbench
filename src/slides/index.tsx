import React from "react";
import PromptEngineeringLecture from "./prompt_engineering_lecture";

function Slides() {
  // For now, we only have one lecture, so let's render it directly.
  // Later, this component could list available slideshows.
  return <PromptEngineeringLecture />;
}

export default Slides; 